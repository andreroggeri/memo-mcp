apiVersion: v1
kind: Service
metadata:
  name: {{ include "memo-mcp.fullname" . }}
  labels:
    {{- include "memo-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memo-mcp.selectorLabels" . | nindent 4 }}
